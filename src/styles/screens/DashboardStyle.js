import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    fontSize: 26,
    fontFamily: 'PoppinsBold',
    position: 'absolute',
    left: 30,
    top: 90,
  },
});

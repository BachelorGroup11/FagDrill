import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		alignItems: 'center',
	},
	headerContainer: {},
	resultaterText: {
		fontSize: 32,
		fontFamily: 'PoppinsBold',
		position: 'absolute',
		top: 44,
		left: 32,
	},
});
